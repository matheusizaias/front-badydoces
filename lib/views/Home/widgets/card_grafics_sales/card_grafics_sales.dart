import 'package:flutter/material.dart';

class CardGraficsSalesWidget extends StatelessWidget {
  const CardGraficsSalesWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
